# 2026-08-25

## ETL 真实导入完整验证（08:30，api 1.0.21，远端 f052611）
- 用户确认"需要"补 staging 唯一索引迁移 + 跑通真实导入。三个真 bug 修复：

1. **迁移 029 (xrefs upsert 42P10 根因修正)**:
   - 之前误判"xrefs_stage 缺约束"; 实际 cross_references 现有 uq_xrefs_brand_oem3=(oem_brand,oem_no_3) WHERE is_discontinued=false
     与 ON CONFLICT (product_id,oem_brand,oem_no_3) WHERE oem_brand IS NOT NULL AND oem_no_3 IS NOT NULL **不匹配**
     (Postgres ON CONFLICT 要求列+谓词与唯一索引完全一致)。
   - 新增 uq_xrefs_product_brand_oem3 (12.5M 行 0 重复, psql 直接应用)。

2. **xrefs 行级错误行号 bug**: 实测 2 错误都显示"行2"。
   - 根因① ProcessXrefBatchAsync 调用传 lastCommittedBatchId(已提交行数) 当 batchStartLineNo
   - 根因② foreach 手动 idx++ 在末尾, 错误分支 continue 跳过 → 行号错乱
   - 修复: 传 lineNo-batchLines.Count + foreach (jsonLine,idx) 索引元组。验证 行2/行3 精确 ✅

3. **products oem_no_display 容错**: 模板标"可选"但代码 doc.GetProperty 强制 → KeyNotFoundException 整批失败。
   - 修复: GetStringOrNull ?? 派生值 oemNorm; type 同改。验证 completed ✅

- 白名单补位瞬时降级: ETL 导入+建索引期间请求超时被取消(OperationCanceledException) → 降级为空, 非代码 bug, 空闲恢复 (aaf→CT 089038 134ms)。
- 测试数据已清理 (TESTBRAND 删除, MR00000001.oem_no_display 恢复 A0000000); Meili 残留 TEST-ROW1 (增量同步快照, 不可见, 记录)。
- **教训**:
  - push_fix.py: Python subprocess shell=True 用 cmd.exe, 路径必须 F:\ (非 /f/); git 输出 GBK → encoding="gbk"
  - git reset --hard origin/master 会丢未推送提交 → reflog + cherry-pick 恢复 (本次 173811a→28b7ebc)
  - 仓库名 = longxiaocao/sakurafilte (注意拼写, 非 sakurafilter!)

## API 文档 500 修复 + 数据导入入口（08:55，api 1.0.23 / web 1.0.15，远端 87d5743）
- 用户反馈: ①运维中心 API 文档报错 ②找不到导入入口。
- ①swagger 500 根因: P0 新增 POST /admin/etl/upload 用 [FromForm] IFormFile → Swashbuckle 6.6.2 对 [FromForm]+IFormFile 组合抛 SwaggerGeneratorException → 整个 swagger.json 生成失败。修复: 去 [FromForm] (minimal API 对 IFormFile 自动 form 绑定, 与 dict import-xlsx 无注解一致) + .Accepts<IFormFile>('multipart/form-data')。验证: swagger 200, 159 端点, upload requestBody multipart/form-data 正确, 上传实测正常。
- ②入口: 菜单恢复独立'数据导入' (priority 6.8 高优区, /admin/etl redirect → /admin/ops?tab=etl); tab.etl 文案 'ETL 触发与监控'→'数据导入'; nav.importData 新增。
- **教训**: docker-compose.prod.yml 历史 reset/cherry-pick 造成文件版本错乱 (646 行 diff 全文件重写, 内容等价服务完整); 后续 sed 版本号前先 git diff 确认。
- 教训: [FromForm]+IFormFile 是 Swashbuckle 已知坑, minimal API 文件上传**不加 [FromForm]**。

## 实体智能识别 + CI 修复（09:10，api 1.0.25 / web 1.0.16，远端 8fe47ff）
- 用户澄清: 困惑的是**实体三选** (products/xrefs/apps) 不是模式; 希望统一智能入口。
- EtlEntityDetector: 上传后读文件内容识别实体 (apps: machine_brand/machine_model; xrefs: oem_brand/oem_no_3; products: type/oem_no_normalized/oem_no_display), 特征打分最高者胜; jsonl 读前 10 行 keys, xlsx 遍历所有 sheet 合并表头。
- **关键: upload 顺序改 识别→转换** — 先 Detect 再用识别结果 ConvertAsync; 原按前端传的 entity 找工作表, 传错即转换失败 (xlsx 模板多 sheet 按实体命名)。
- 前端: 上传后自动填充实体 + 提示; radio 保留(下载模板用) + 提示文案。
- CI: ci.yml/e2e.yml 加 DOTNET_ROLL_FORWARD=LatestMajor (修复 .NET 10 抢占 8.0 build 失败)。
- 验证: 3 种 jsonl + 2 种 xlsx 模板全部识别正确 (故意传错 entity 也识别对, autoDetected=true)。
- **教训: 之前 push 一直带 [ci skip] 导致 87d5743 等修复从未跑 CI; 用户看到 3 个失败邮件是昨天 17:13 的 commit (db38966 docs 提交 build 失败), 4fabb5d 是无效 hash 误报**。

## CI 全绿（09:30，远端 22f467c）
- 三轮修复: ①ci.yml/e2e.yml 加 DOTNET_ROLL_FORWARD=LatestMajor (setup 修复) ②029 迁移补 idempotent 头注释 (CI 头注释检查: migrations/*.sql 第一行必须含 '一次性脚本,不可重跑' 或 'idempotent') ③Backend Build & Smoke Test 通过。
- 最终 7/7 全绿: setup/Backend Build & Smoke Test/e2e/frontend-contract/OpenAPI/Frontend/Coverage。
- **教训: 之前 push 一直带 [ci skip] 导致多轮修复从未跑 CI; 用户看到 3 封失败邮件全是昨天 17:13 commit 的**。此后推送**必须去掉 [ci skip]** 让 CI 验证。

## 统一模板（09:35，api 1.0.27 / web 1.0.18，远端 7765192）
- 用户明确: 模板也要统一 — 一个模板文件, 填完系统自动识别, 全程不选类型。
- EtlTemplateGenerator.Build: 空/非法实体 → 3 sheet 统一模板 (products/xrefs/apps 各一, 表头+批注+示例行); 单实体参数兼容。
- template 端点 entity 空 → 统一模板 (文件名 sakurafilter-import-template.xlsx)。
- EtlEntityDetector 升级: xlsx **逐 sheet 打分, 仅'有数据行'的 sheet 参与** (跳过表头 Row1+示例行 Row2, Row3 起有非空 cell) — 修复 3 sheet 空表头打平误判 apps。
- 前端: 模板下载单按钮; 删除实体 radio/dropdown; 上传后 '已识别: XXX' tag。
- 验证: 统一模板 3 sheet 200/14.9KB; 空模板→products; 填 xrefs Row3→xrefs+转换成功; jsonl xrefs 正常。
- **教训: xlsx 识别不能只看表头 (统一模板 3 sheet 都有表头), 必须看数据行**。

## 上线前全面审查（10:20）
- 导入边界实测 8 项全过: 空文件400/非法扩展400/坏JSONL(导入时行级)/51MB→413/未授权401/特殊字符OK/坏xlsx 400/重复数据 upsert 正常。
- **高危 3 项（上线前必改）**:
  ① ETL advisory lock 失效: TryAcquireAdvisoryLockAsync 用 pg_try_advisory_xact_lock 但 ImportProductsAsync(L865)/ReindexAll(L1525) 无活动事务 → autocommit 下锁立即释放, 并发导入互斥形同虚设; ReindexFromIdAsync(L1613) 用 pg_try_advisory_lock 会话级+finally unlock 是正确范式。
  ② backup-db.sh 无自动调度 (仅手工)。
  ③ 迁移无自动应用通道 (db-init/db-migrate 禁用后 029 靠手工 psql)。
- **中危**: /demo 公开路由带 admin/admin123 提示; NuGetAudit=false; Turnstile 生产未配(fail-open); /swagger 公网可访问; refresh token 无重用检测+表无清理; JWT 存 localStorage; VITE_SAFE_REDIRECT_HOSTS 占位符; OFFSET 深翻页。
- oem2 降级图容器重建丢 (已知, 主路径 MinIO 不受影响, AggregateSearchView L290 注释)。
- 结论: 功能就绪度✅ (核心流程全通/691单测+39集成全绿/CI 7项全绿), 运维就绪度⚠️ (备份/迁移/并发锁 3 高危)。

## 上线前高危修复（11:00，api 1.0.28 / web 1.0.20，远端 e6b1302）
- codex 复核确认我的报告 + 补充关键点: 三 ETL 分键 (7740001/2/3) 即使会话级也无法互相排斥, products full-load 清三表时其它导入并发写。
- **P0 全局互斥锁**: 统一 EtlGlobalLockKey=7740000 (EtlImportService 5 入口 + AdminProductService 4 处 + IndexReplayWorker);
  事务级 xact lock → 会话级 pg_try_advisory_lock + IAsyncDisposable 句柄显式 unlock (Npgsql 池复用连接, 不能靠连接关闭释放)。
  PG 跨会话实测 A持锁 B=false / A释放 B=true ✅。
- **P1 迁移通道**: scripts/migrate.sh (schema_migrations 表 + --baseline 防首次重复执行, deploy-prod.sh 集成);
  **备份异机**: backup-db.sh --upload 上传 MinIO 桶。
- **P2 安全**: NuGetAudit=true; refresh 限流; /demo 生产守卫 (PROD 跳首页)。
- 实测: baseline 标记 19 迁移; MR00000001 oem_no_display 测试污染已恢复。
- **教训: 新增迁移必须走 migrate.sh 基线流程; 上线后 deploy-prod.sh 自动应用新迁移**。

## CI 全绿确认（11:20，远端 10f2195）
- 锁改造后集成测试失败 (测试用旧 key 7740001) → 同步测试 key 7740000 后 CI 9/9 全绿。
- 教训: **改锁 key 必须同步集成测试里的硬编码 key** (AdminProductServiceIntegrationTests/IndexReplayWorkerLockMechanismTests)。

## CI/E2E 失败排查 + 单 commit 推送（12:20，远端 fc80dfb）
- 用户报告 CI/E2E 失败 → 根因: **push_fix.py 用 Contents API 逐文件 PUT, 每文件 1 个 commit → 中间态 commit (文件不全) 编译失败** (53e3dc0b/37987409 E2E setup failure)。最终态 10f2195 全绿, 当前无代码问题。
- **新工具 scripts/push-single-commit.py**: Git Data API (blob→tree→commit→ref) 单 commit 推送, CI 只跑一次完整代码。实测推送 fc80dfb 成功 + CI 全绿。
- git push (ghfast 镜像) 卡 11 分钟被杀 → **以后推送统一用 push-single-commit.py** (api.github.com 直连, ~10s)。
- 教训: 推送到 GitHub 必须**单 commit** (Contents API 逐文件 PUT 会产生中间态失败邮件)。

## codex v2 审核 5 项阻断修复（13:00，远端 961aa5b）
- ①备份: endpoint 容器网络 minio:9000 (原 127.0.0.1:55900 不可达) + mc cp stdin 不支持改 docker cp 进容器 + 失败 exit 1 + BACKUP_S3_ENDPOINT 支持异机 R2。实测上传 734MiB 成功。
- ②deploy 顺序重写 8 步: 备份→停API→迁移(失败退出)→启动→健康检查(3 次重试失败退出)。
- ③migrate.sh EF 通道: docker run API 镜像 --migrate-db (网络名动态探测 {项目}_sakura-net), EF 先→SQL 后。实测幂等成功。
- ④baseline 需 --confirm + 4 张关键表结构校验。
- ⑤AdvisoryLockHandle 释放失败 LogWarning (持 ILogger, 不再静默)。
- **教训: docker compose 网络名带项目前缀 (sakurafilter-real_sakura-net), 脚本要动态探测; mc cp 不支持 /dev/stdin**。

## codex v3 发布链修复（13:40，远端 4279957）
- ①构建前置: deploy 9 步, build api 先于迁移 (EF 用新镜像, 新 C# migration 才生效)。
- ②EF 硬阻断: migrate.sh 条件不满足 exit 1 (SKIP_EF=1 应急)。
- ③REQUIRE_BACKUP=1 默认阻断发布, REQUIRE_BACKUP=0 应急跳过。
- ④.env.prod.example 加 BACKUP_S3_* (生产必须 R2/异地); --upload 无 mc 也 exit 1。
- ⑤迁移后结构验证: schema_migrations 19/19 + EF 历史表 + 关键列, 失败 exit 1。
- 实测: migrate.sh 全链路 (EF 幂等 + SQL + 结构验证全过); CI 全绿。
- **教训: 发布顺序必须是 构建→迁移→启动; 备份策略要可配置 (REQUIRE_BACKUP)**。

## codex v4 环境侧修复（15:00，远端 2eabbee）
- ①REQUIRE_REMOTE_BACKUP=1 默认: 本机 endpoint (minio:9000/127.0.0.1) 拒绝视为备份成功 (exit 1), 生产未配 R2 阻断发布; REQUIRE_REMOTE_BACKUP=0 应急。实测: 默认拒绝/应急上传成功。
- ②业务探针: 断言聚合搜索 JSON 含 hits (不只验 HTTP); error 则 FAIL。
- ③迁移验证: schema_migrations 无幽灵记录 (DB 有记录但目录无文件 → 阻断)。实测插入 999_ghost.sql 检测成功。
- **剩余环境配置 (用户操作): .env.prod 配 BACKUP_S3_ENDPOINT/USER/PASS (R2), 生产演练时做一次恢复演练**。

## 体验优化（16:00，远端 628eda1，api 1.0.29 / web 1.0.21）
- 用户反馈 4 项: ①数据导入重复 ②白名单留白+慢 ③字典慢 ④缺备份按钮。
- 去重: /admin/etl 新路由直达 AdminEtlView; AdminOpsView 移除 etl tab + 加 backup tab。
- 白名单: 去 min-height:600px (消除下方空白) + brand 列表加 skeleton。
- 字典: DictManagerLayout 已有 SkeletonCard (无需改, 慢是后端 API 全量, 留作后续分页)。
- 备份: AdminBackupEndpoints (list/script-info) + compose ./_backups:/backups:ro mount +
  AdminBackupView (列表+复制命令+目录缺失态)。exec 端点不做 (API 无 docker CLI 风险高)。

## codex v5 异机备份读取链修复（16:45，远端 54e25ae）
- P1: BACKUP_S3_ENDPOINT/USER/PASS 只读环境变量不读 .env.prod → 用户写 .env.prod 读不到 → 持续误判阻断。
- 修复: backup-db.sh 读取链 = 环境变量优先 → .env.prod grep 兜底; deploy export REQUIRE_REMOTE_BACKUP。
- 实测: 只写 .env.prod 不 export → 脚本读到 fake-r2 endpoint 并尝试上传 (连接失败 exit 1) 读取链验证通过。
- **教训: bash 子进程读 env 文件变量必须显式 grep/source, 不能假设 compose --env-file 会导出**。

## admin 菜单隔离修复（22:40，远端 4e57447，web 1.0.23）
- 用户反馈: 公开搜索页 /public/search 显示完整 admin 布局。
- A: admin 菜单仅 isAuthenticated && isAdminPath (/admin/*) 显示; 公开页不再污染。
- C: 高级搜索 (adv-search, /public/search) 是公共功能, 移出 admin 菜单到公共区。
- goAdmin (用户下拉) 改跳 /admin/ops; 公开页 admin 经用户菜单回后台。

## 对比抽屉不再自动弹（22:50，远端 6304b95，web 1.0.24）
- 用户反馈: 详情页点加入对比跳 /public/search?compare=ID 后, 只加 1 个就自动开抽屉。
- 根因: onMounted 解析 ?compare= 后 compareOpen.value=true (无论数量)。
- 修复: 去掉该行 — URL 带 compare 时只填入 compareIds (摘要条显示), 预拉产品详情, 不开抽屉; 用户点"查看对比"才展开。

## 详情页加入对比不再跳（23:10，远端 8057baf，web 1.0.25）
- 用户反馈: 详情页点加入对比强制跳 /public/search 割裂, 期望从哪来回哪去。
- 修复: 详情页 addToCompare 写 sessionStorage + toast + 不跳; PublicSearchView 加 watch 同步持久化。

## 全局浮动对比栏（23:40，远端 3690ee3，web 1.0.26）
- 用户反馈: 加入对比后没有常驻 N/6 控件, 没地方点"查看对比"。
- 实现: useCompareStore (模块级单例, sessionStorage 持久化) + CompareFloatingBar (右下角浮动徽标,
  挂 App.vue 所有页面可见, 点击展开对比抽屉); PublicSearchView/PublicProductView 状态→store。
- **教训: element-plus icons 无 Compare 图标 (用 Collection 替代); 全局共享状态用 composable 单例而非各页 ref**。

## 对比表格 3+ 产品显示（23:55，远端 147d78a，web 1.0.27）
- 用户反馈: 3 个对比只显示 2 个。
- 修复: 抽屉 900→1280px, 字段列 160→140px, 产品列 minmax 180→150px。3 个产品 380px/列, 6 个 190px/列 一屏内。

## 对比表格重写（00:10，远端 2a30ba3，web 1.0.28）
- 用户反馈: 3 个对比只显示 2 个仍未解决 (浏览器缓存 main.js), 不要左右移, 要"只看不同项"开关。
- 重写: 删左右移; 加 diffOnly el-switch; 列宽自适应 (2→220 3→180 4→150 5→130 6→115); 顶部产品数提示。
- 验证: dist main.js 含 diffOnly + minmax(115px)。
- **教训: vite 单文件 main.js 输出无 hash, 浏览器长期缓存, 用户需 dev tools disable cache 强刷**。

## store.products 竞态修复（00:25，远端 9fa7f88，web 1.0.29）
- 用户反馈 + 截图证据: "产品对比 (3)"标题但只渲染 2 个产品列。
- **真正根因 (我之前完全跑偏)**: useCompareStore.add() 每次 add 都 fire-and-forget 触发 fetchProducts(), 
  N 次并发请求各自用不同 idsSnapshot, 最后返回的覆盖 products, 但 map 只有最早请求的 1-2 个详情。
  修复: AbortController 取消旧请求 + 最新 ids 快照重新拉。
- **教训: 标题(3)/渲染(2) 的差异 = products 数组缺失 = fetch 竞态, 不是列宽问题**。

## 重复 × 按钮修复（00:40，远端 e4c895f，web 1.0.30）
- 用户反馈: 每个产品列头 2 个红叉。
- 根因: 上一轮 "删左右移" 的 Edit 模板嵌套不完整, 容器外多写了 1 个 el-button ×。
- 修复: 删冗余 × 按钮, 只留 1 个 (移除按钮)。
