test(e2e): P2 补充 BD.21 Auth 完整流程 (me/refresh/logout) 测试

 WHY: /api/auth/me, /api/auth/refresh, /api/auth/logout 三个核心认证端点未测
   - me: 验证 JWT claims 解析 + 当前用户信息返回
   - refresh: 验证 refresh token 一次性使用 (revoke 旧 + issue 新)
   - logout: 验证 refresh token 撤销后无法再 refresh

 测试项 (5 项):
   21.1 /api/auth/me → 200 + username=admin + role=admin
   21.2 /api/auth/refresh → 200 + 新 accessToken + 新 refreshToken
   21.3 旧 refresh token 二次使用 → 401 (一次性使用机制)
   21.4 /api/auth/logout → 200 (撤销新 refresh token)
   21.5 logout 后再 refresh → 401 (验证撤销生效)

 改动:
   - test_backend_deep_water 签名增加 login_resp 参数 (获取 admin refresh token)
   - main 调用处同步更新

 结果: 136 → 141 PASS / 0 FAIL / 0 WARN (连续两次稳定)
