# 客户演示数据准备清单（2026-08-24 更新）

> 基于生产环境（sakurafilter-real）实测，以下为可直接照做的演示素材。

## 一、演示账号

- 管理后台：`admin` / `Sakura#be1281`
- ⚠️ 系统已启用 Cloudflare Turnstile 人机验证：**浏览器演示需能访问 cloudflare 验证服务**（离线/内网隔离环境会卡在登录页）。

## 二、批量查询 OEM（5 个，已实测 5/5 全命中）

```
U0000014   → FRAM       OIL FILTER
V0000015   → KTM        EXHAUST FILTER
W0000016   → BLUE PRINT ACTIVATED CARBON FILTER
X0000017   → ENERGY     HYDRAULIC FILTER
Y0000018   → SOGEFI     OIL FILTER
```

- 5 个 OEM 均命中同一产品（pid=2，MR00000002），可演示"**一个产品挂多个 OEM 品牌**"（客户产品常有此场景）。
- 需要演示"批量查询返回多个不同产品"时，可追加下列任意样本（均实测命中、不同产品）：

```
R0BBD043 → TOPRAN OIL FILTER      / V0B08721 → MAHLE OIL SEPARATOR
U0C9D1F0 → FRAM ACTIVATED CARBON  / O04E859C → ENERGY HYDRAULIC FILTER
J11FCF91 → AAF CABIN AIR FILTER   / P0DB5003 → COOPERSFIAAM AIR DRYER
V05728DB → LUBER-FINER AIR/OIL SEP / V12361C7 → FEBI FUEL FILTER
K1053108 → WIX FILTERS HYDRAULIC  / A0593A86 → PBR COOLANT FILTER
ZZ9999999 → 故意未命中（演示 miss 提示）
```

## 三、带主图（U0000014）✅ 已验证

| 项 | 状态 |
|---|---|
| R2 主图（`/api/public/images/products/primary/U0000014/U0000014-1.jpg`） | ✅ HTTP 200（23KB） |
| 详情页 `/product/U0000014` | ✅ 主图 + 详情图 + 完整参数（D1=129.6 / H1=465.9） |
| 搜索卡片图 | ✅ 搜 5 个 OEM 任一，卡片均显示主图（同一产品共用） |
| 尺寸标注线 | ✅ 已开启（黑白极简、线段外挪不挡图）；可在管理后台图片 Tab 关闭 |

- **搜索卡片图机制**：搜索结果按产品（mr1）回填 `primaryImageUrl`，5 个 OEM 属同一产品，因此搜任一个都显示同一张主图。
- 图片来自 Flickr/Wikimedia 公开照片（仅演示验证用），**正式演示前建议替换为客户实拍产品图**（管理后台产品编辑 → 图片 Tab 上传，或 `POST /api/admin/products/{mr1}/images/primary`，X-Admin-Token 鉴权）。

## 四、演示建议顺序

1. **登录**管理后台（验证账号可用）
2. **批量查询**：贴入 5 个 OEM（含命中 + 未命中样本，展示结果/提示）
3. **搜索**：输入 `U0000014` 看卡片图与详情页（主图 + 参数表）
4. **详情页**：展示尺寸参数表 + 标注线（若开启）
5. **机型目录 / 聚合搜索**（可选）：`FRAM`、`OIL`、`FUEL`、`KTM`、`MAHLE`

## 五、注意事项

- 生产存储为 Cloudflare R2（bucket `sakurafilter-images-prod`），非 MinIO。
- 遗留（与演示无关）：R2 桶 34 个孤儿对象待清理；100 万产品中仅演示产品配了图，其余显示类型缩写占位块（正常表现）。
